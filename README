簡單的git push…
簡單的git pull…

# 加入所有已變更的檔案
git add .
# 加上註解
git commit -m '註解內容'
# 丟資料上github
git push

如果本機檔案修改過或檔案遺失
可以先透過
git checkout
確認哪些檔案有變更
再用
git checkout filename
取回

# 建立local branch
git branch <分支名>
# 切換到所需的分支
git checkout <分支名>
如果現有的分支有修改的話會無法切換

# 將local branch push到github上
git push origin <分支名>
之後的push就都會push到分支上

